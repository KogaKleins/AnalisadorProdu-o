pandas==2.3.0
numpy==2.3.0
pdfplumber==0.11.7
fpdf
unidecode
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.2
charset-normalizer==3.4.2
# Dependências do Windows/Linux (Tkinter já vem com Python padrão)
# tkinter
# Dependências de logging, pathlib, subprocess, etc. já são da biblioteca padrão
